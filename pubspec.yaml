name: hive_eg
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^2.0.4
  hive_flutter:
    git:
      url: https://github.com/jinosh05/hive_flutter
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.0
  build_runner: ^2.0.4
  analyzer: 1.4.0

flutter:
  uses-material-design: true
